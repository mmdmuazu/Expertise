// import { createClient } from "@supabase/supabase-js";

// // Ensure you set these environment variables in your .env.local or deployment secrets
// const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL || "https://amir.com";
// const supabaseAnonKey =
//   process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || "https://amir.com";

// export const supabase = createClient(supabaseUrl, supabaseAnonKey);
