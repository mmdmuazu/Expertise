/** @type {import('next').NextConfig} */
const nextConfig = {
  // In Next.js 15 the App Router is stable and enabled by default.
  // The `experimental.appDir` flag is no longer recognized.
  reactStrictMode: true,
};
module.exports = nextConfig;
